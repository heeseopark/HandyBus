#spring.datasource.url=jdbc:postgresql://localhost:5432/handyBusDB
#spring.datasource.username=heeseopark
#spring.datasource.password=heeseo!)((2
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/handybus
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
logging.level:
  org.hibernate.SQL: debug
